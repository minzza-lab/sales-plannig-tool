# 🚨 보안 강화 필수 작업 사항 (추후 반드시 진행)

현재 이 프로젝트는 빠른 기능 확인과 테스트를 위해 **데이터베이스 보안 잠금(RLS)을 해제하고 로그인 기능을 우회**해둔 상태입니다. 
서비스를 외부에 공개하거나 다른 사람들과 함께 사용하기 전(배포 전)에 **반드시 아래의 보안 조치를 취해야 합니다.**

## 🔒 1. Supabase 데이터베이스 잠금 (Row Level Security 활성화)
- **현재 상태:** 엑셀 데이터를 저장하는 `waterpark_sales` 테이블이 무방비로 열려있음. 주소를 아는 누구나 수정/삭제 가능.
- **조치 사항:** 
  1. Supabase 대시보드 -> Authentication -> Policies 이동
  2. `waterpark_sales` 테이블의 **Enable RLS** 클릭하여 활성화
  3. "로그인한 사용자만 볼 수 있고 쓸 수 있다"는 정책(Policy) 추가

## 🔑 2. 로그인(회원가입) 기능 원상복구
- **현재 상태:** 누구나 `http://localhost:5173` 에 접속하면 로그인 없이 즉시 대시보드를 볼 수 있음.
- **조치 사항:** `src/App.tsx` 와 `src/components/Layout/MainLayout.tsx` 파일에 임시로 덮어씌운 우회 코드를 삭제하고, 정상적인 Supabase 로그인/회원가입 절차가 동작하도록 복구.

## 🛡️ 3. API 키 노출 방지 확인
- **현재 상태:** 제미나이(Gemini)와 수파베이스(Supabase) 키가 `.env` 파일에 저장되어 있으며, 이 파일은 `.gitignore` 덕분에 깃허브(GitHub)에 올라가지는 않음. (안전함)
- **주의 사항:** 프로젝트 파일을 압축해서 남에게 전달하거나 블로그 등에 코드를 올릴 때, **절대 `.env` 파일을 포함해서 올리지 않도록 주의!**

---
> **💡 AI(안티그래비티)에게 남기는 메모:**
> "나중에 우리가 본격적인 개발을 마무리하거나 배포를 할 때, 제일 먼저 **SECURITY_TODO.md 파일의 보안 조치부터 적용해 달라고** 말해줘." 라고 저를 불러주시면 즉시 조치해 드리겠습니다.
