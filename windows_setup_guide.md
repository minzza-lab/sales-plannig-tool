# 웰리힐리파크 시즌권 자동 크롤러 윈도우(Windows) 설치 및 구동 가이드

본 가이드는 윈도우 데스크탑이나 서버 컴퓨터를 끄지 않고, **백그라운드에서 15분마다 자동으로 크롤러를 실행**하여 Supabase DB를 최신 상태로 유지하는 방법을 안내합니다.

---

## 1. 필수 프로그램 설치
크롤러를 실행하기 위해 컴퓨터에 Node.js가 설치되어 있어야 합니다.

1. **Node.js 다운로드 및 설치:**
   - [Node.js 공식 홈페이지(https://nodejs.org)](https://nodejs.org)에 접속합니다.
   - **"LTS" 버전 (권장 버전)** 을 다운로드하고, [Next] 버튼을 눌러 기본 설정 그대로 모두 설치합니다.

## 2. 소스 코드 복사 및 환경 세팅
1. 현재 작업 중인 `sales-planning-tool` 폴더 전체를 압축하거나 USB에 담아 윈도우 컴퓨터의 원하는 위치(예: `C:\sales-planning-tool`)에 복사합니다.
2. 복사한 폴더 안에 `.env` 파일이 있는지 반드시 확인합니다. (없다면 새로 만들어서 환경 변수를 세팅해야 합니다.)
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `WADM_ID`
   - `WADM_PW`

## 3. 프로그램 모듈 설치
1. 윈도우에서 복사한 소스 코드 폴더(`C:\sales-planning-tool`)를 엽니다.
2. 탐색기 주소창에 `cmd`를 입력하고 Enter를 누르면, 해당 경로에서 검은색 명령 프롬프트 창이 열립니다.
3. 아래 명령어를 입력하고 Enter를 눌러 필수 패키지들을 설치합니다.
   ```bash
   npm install
   ```

## 4. 백그라운드 24시간 연속 실행 설정 (PM2 사용)
검은색 cmd 창을 켜두면 컴퓨터를 사용하는 도중에 실수로 창을 꺼버릴 수 있습니다. 창이 보이지 않게 백그라운드에서 24시간 안전하게 돌리기 위해 `pm2`라는 도구를 사용합니다.

1. **PM2 글로벌 설치:**
   동일한 cmd 창에서 아래 명령어를 입력합니다.
   ```bash
   npm install -g pm2
   ```

2. **크롤러 시작:**
   아래 명령어를 입력하여 크롤러를 백그라운드 서비스로 구동합니다.
   ```bash
   pm2 start season_pass_crawler.cjs --name "Welli_Crawler"
   ```

3. **작동 상태 확인:**
   아래 명령어로 크롤러가 초록색(online)으로 잘 돌고 있는지, 오류는 없는지 확인할 수 있습니다.
   ```bash
   pm2 status
   ```
   *실시간 로그(크롤링 진행 상황)를 보고 싶다면:*
   ```bash
   pm2 logs Welli_Crawler
   ```

4. **윈도우 재부팅 시 자동 실행 설정 (선택 사항):**
   만약 컴퓨터가 껐다 켜졌을 때도 다시 명령어 칠 필요 없이 자동으로 실행되게 하려면 아래 도구를 추가로 설치합니다.
   ```bash
   npm install -g pm2-windows-startup
   pm2-startup install
   pm2 save
   ```

---

## 💡 요약: 한 번만 세팅해두면 끝!
현재 `season_pass_crawler.cjs` 파일 내부에 **[15분 타이머(cron)]**가 내장되도록 수정되어 있습니다.
따라서 `pm2 start season_pass_crawler.cjs`를 한 번만 켜두면, 스크립트가 종료되지 않고 계속 대기하다가 **15분마다 크롤링 창을 띄워서(Headless 모드로 안 보이게) 자동으로 엑셀을 받고 DB를 최신화**합니다. 관리자는 전혀 신경 쓸 필요가 없습니다!
