# 2026-05-10 작업 요약 (시즌패스 엑셀 및 PWA 모바일 설치)

본 문서는 Windows 환경에서 작업 후 MacBook으로 원활하게 인수인계하기 위해 작성된 작업 요약본입니다.

## 1. 시즌권 판매 현황 엑셀 다운로드 오류 완벽 해결
- **데이터 덮어쓰기 및 누락 현상 수정:**
  - 엑셀 양식 내 수식(`Formula`)이 있는 '소계/합계' 셀을 자동으로 감지하여 초기화(0으로 덮어쓰기)하지 않도록 보호 로직 추가.
  - 대시보드의 데이터를 엑셀에 채울 때, **띄어쓰기가 포함된 권종명(예: `대 인` vs `대인`)으로 인해 항목이 별개로 나뉘어 매칭에 실패하고 누락되던 크리티컬 버그(210매 표기 오류)**를 근본적으로 수정. (모든 띄어쓰기를 완전히 제거하고 병합 후 매칭하도록 수정하여 212매 정상 표기)
- **시간 표기 버그 수정:**
  - 기간 합산을 위해 임시로 `23:59:59`로 설정하던 로직이, 상단의 현재 다운로드 시간 표기에 영향을 주는 버그(참조(Reference) 변형 오류)를 복제(Clone) 방식을 사용하여 수정 완료. (이제 `08:52` 등 다운로드 시점의 정확한 현재 시간이 표기됨)

## 2. 모바일 기기(아이폰/안드로이드) 웹 앱(PWA) 설치 기능 추가
- **웹 앱 환경(PWA) 세팅:**
  - `public/manifest.json` 추가, `index.html`에 iOS 호환 메타태그 및 테마 색상 설정.
  - 모바일 홈 화면 설치 시 표시될 앱 아이콘(`icon-192.png`, `icon-512.png`) 자체 생성 및 적용.
  - 이제 스마트폰에서 앱 바로가기를 설치하면 상하단 주소창이 사라지는 전체화면 모드(앱처럼 동작)로 깔끔하게 실행됨.
- **모바일 접속 설치 가이드 UI 추가:**
  - `Login.tsx` 화면 하단에 안드로이드/아이폰 여부를 감지하여 기종에 맞는 설치 가이드 안내 배너 표시.
  - (아이폰: 하단 공유 버튼 > 홈 화면에 추가 / 안드로이드: 메뉴 버튼 > 홈 화면에 추가)
- **최초 접속 시 로그인 강제 이동 보안 강화:**
  - PWA 앱으로 직접 접속하더라도, 세션(로그인 상태)이 없을 경우 어떠한 메뉴도 보여주지 않고 즉시 로그인 화면으로 튕겨지도록 라우팅 방어벽 점검 및 확인.

---

## 💻 백그라운드 크롤러 구동 관련 안내 (Windows ↔ MacBook)
현재 **Windows 컴퓨터에 설치되어 백그라운드(PM2 또는 스케줄러)로 돌아가고 있는 VOC 및 매출 크롤러 봇들**은, Windows 컴퓨터가 켜져 있고 인터넷이 연결되어 있는 한 **MacBook에서의 작업과 전혀 무관하게 24시간 정상적으로 작동**합니다.

크롤러가 긁어온 데이터는 클라우드 데이터베이스(Supabase)에 실시간으로 저장되며, MacBook에서는 단순히 그 저장된 데이터를 불러와서 보여주는 '프론트엔드(대시보드)' 코드를 수정하는 것이기 때문에 **크롤러 시스템에 어떠한 악영향도 주지 않습니다.** 안심하시고 맥북으로 넘어가서 자유롭게 UI 및 프론트엔드 개발 작업을 진행하셔도 됩니다!
